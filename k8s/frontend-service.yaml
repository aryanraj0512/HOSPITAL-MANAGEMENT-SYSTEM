apiVersion: v1
kind: Service
metadata:
  name: frontend-hms
  labels:
    app: frontend-hms
spec:
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: frontend-hms
