apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-hms
  labels:
    app: frontend-hms
spec:
  replicas: 2
  selector:
    matchLabels:
      app: frontend-hms
  template:
    metadata:
      labels:
        app: frontend-hms
    spec:
      containers:
      - name: frontend
        image: your-registry/hospital-frontend:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 3000
          name: http
        env:
        - name: REACT_APP_API_URL
          value: "http://backend-hms:5000"
        - name: REACT_APP_ENV
          value: "production"
        livenessProbe:
          httpGet:
            path: /
            port: 3000
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /
            port: 3000
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 2
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "500m"
      imagePullSecrets:
      - name: regcred
